namespace: fleet-default
helm:
  repo: https://rancher.github.io/rodeo	
  chart: rancher-demo
  valuesFiles:
    - "values.yaml"
targetCustomizations:
- name: dev
  clusterSelector:
    matchLabels:
      env: dev
  kustomize:
    dir: clusters/dev